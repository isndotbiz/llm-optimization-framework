═══════════════════════════════════════════════════════════════════════════════
  RESPONSE POST-PROCESSING - QUICK INTEGRATION GUIDE
═══════════════════════════════════════════════════════════════════════════════

STATUS: Core implementation COMPLETE. Manual integration required (10-15 min).

═══════════════════════════════════════════════════════════════════════════════
  STEP 1: ADD POST-PROCESSING METHODS TO AIRouter CLASS
═══════════════════════════════════════════════════════════════════════════════

LOCATION: ai-router.py, line ~710 (after parse_llama_output, before run_model)

ACTION:
1. Open ai-router.py
2. Find line ~710: "def run_model(self, model_id, model_data, prompt):"
3. Position cursor BEFORE this line
4. Open post_processing_methods.txt
5. Copy ALL contents (7 methods, ~170 lines)
6. Paste into ai-router.py
7. Save

VERIFY: You should now have these methods in AIRouter class:
  - post_process_response()
  - _save_response_to_file()
  - _extract_and_save_code_blocks()
  - _show_statistics()
  - _copy_to_clipboard()
  - _export_as_markdown()
  - _list_saved_responses()

═══════════════════════════════════════════════════════════════════════════════
  STEP 2: UPDATE run_model() TO STORE AND PROMPT FOR POST-PROCESSING
═══════════════════════════════════════════════════════════════════════════════

LOCATION: ai-router.py, run_model() method, around line 716-726

FIND THIS CODE:
────────────────────────────────────────────────────────────────────────────
    if model_data['framework'] == 'mlx':
        response = self.run_mlx_model(model_id, model_data, prompt)
    else:
        response = self.run_llamacpp_model(model_id, model_data, prompt)

    if response:
        response.duration_seconds = time.time() - start_time

    return response
────────────────────────────────────────────────────────────────────────────

REPLACE WITH:
────────────────────────────────────────────────────────────────────────────
    if model_data['framework'] == 'mlx':
        response = self.run_mlx_model(model_id, model_data, prompt)
    else:
        response = self.run_llamacpp_model(model_id, model_data, prompt)

    if response:
        response.duration_seconds = time.time() - start_time

        # Store for post-processing
        if hasattr(response, 'text') and response.text:
            self.last_response = response.text
            self.last_model_name = model_data['name']

            # Ask if user wants to post-process
            if self._confirm(f"\n{Colors.BRIGHT_CYAN}Post-process this response? [y/N]:{Colors.RESET}", default_yes=False):
                self.post_process_response(response.text, model_data['name'])

    return response
────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
  STEP 3: ADD MENU OPTION IN interactive_mode()
═══════════════════════════════════════════════════════════════════════════════

LOCATION: ai-router.py, interactive_mode() method, around line 530-565

PART A: UPDATE MENU DISPLAY
────────────────────────────────────────────────────────────────────────────

FIND:
    print(f"{Colors.BRIGHT_GREEN}[5]{Colors.RESET} 📚 View documentation guides")

    # Bypass mode toggle option
    bypass_status = f"{Colors.BRIGHT_GREEN}ON{Colors.RESET}" if self.bypass_mode else f"{Colors.BRIGHT_RED}OFF{Colors.RESET}"
    print(f"{Colors.BRIGHT_GREEN}[6]{Colors.RESET} 🔓 Toggle Auto-Yes Mode (Currently: {bypass_status})")

    print(f"{Colors.BRIGHT_GREEN}[7]{Colors.RESET} 🚪 Exit")

REPLACE WITH:
    print(f"{Colors.BRIGHT_GREEN}[5]{Colors.RESET} 📚 View documentation guides")

    # Bypass mode toggle option
    bypass_status = f"{Colors.BRIGHT_GREEN}ON{Colors.RESET}" if self.bypass_mode else f"{Colors.BRIGHT_RED}OFF{Colors.RESET}"
    print(f"{Colors.BRIGHT_GREEN}[6]{Colors.RESET} 🔓 Toggle Auto-Yes Mode (Currently: {bypass_status})")

    # Post-processing option
    post_process_status = f"{Colors.GREEN}available{Colors.RESET}" if self.last_response else f"{Colors.DIM}none{Colors.RESET}"
    print(f"{Colors.BRIGHT_GREEN}[7]{Colors.RESET} ⚙️  Post-process last response (Status: {post_process_status})")

    print(f"{Colors.BRIGHT_GREEN}[8]{Colors.RESET} 🚪 Exit")
────────────────────────────────────────────────────────────────────────────

PART B: UPDATE MENU PROMPT
────────────────────────────────────────────────────────────────────────────

FIND:
    choice = input(f"\n{Colors.BRIGHT_YELLOW}Enter choice [1-7]: {Colors.RESET}").strip()

REPLACE WITH:
    choice = input(f"\n{Colors.BRIGHT_YELLOW}Enter choice [1-8]: {Colors.RESET}").strip()
────────────────────────────────────────────────────────────────────────────

PART C: ADD HANDLER IN IF/ELIF CHAIN
────────────────────────────────────────────────────────────────────────────

FIND:
    elif choice == "6":
        self.toggle_bypass_mode()
    elif choice == "7":
        print(f"\n{Colors.BRIGHT_GREEN}Goodbye!{Colors.RESET}\n")
        sys.exit(0)

REPLACE WITH:
    elif choice == "6":
        self.toggle_bypass_mode()
    elif choice == "7":
        self.post_process_response()
    elif choice == "8":
        print(f"\n{Colors.BRIGHT_GREEN}Goodbye!{Colors.RESET}\n")
        sys.exit(0)
────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
  STEP 4: SAVE AND TEST
═══════════════════════════════════════════════════════════════════════════════

1. Save ai-router.py
2. Run: python ai-router.py
3. Test auto-select or manual model
4. Generate a response
5. When prompted "Post-process this response? [y/N]:", type 'y'
6. Test menu options:
   [1] Save to file
   [2] Extract code blocks (if response has code)
   [3] Show statistics
   [4] Copy to clipboard
   [5] Export as markdown
   [6] List saved responses
   [0] Continue
7. Return to main menu
8. Check option [7] "Post-process last response" works
9. Verify files created in D:\models\outputs\

═══════════════════════════════════════════════════════════════════════════════
  OPTIONAL: INSTALL PYPERCLIP FOR CLIPBOARD SUPPORT
═══════════════════════════════════════════════════════════════════════════════

Run: pip install pyperclip

This enables menu option [4] "Copy to clipboard"
Without it, you'll see: "⚠ Clipboard functionality not available."

═══════════════════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

ERROR: "ModuleNotFoundError: No module named 'response_processor'"
FIX: Ensure response_processor.py is in D:\models\ directory

ERROR: "AttributeError: 'NoneType' object has no attribute 'text'"
FIX: Make sure run_model() was updated correctly (Step 2)

ERROR: "No such file or directory: 'outputs'"
FIX: Directory should be auto-created. Manually run: mkdir D:\models\outputs

ERROR: Menu option [7] shows "none" status
FIX: Generate at least one response first

═══════════════════════════════════════════════════════════════════════════════
  VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

□ Import statement present (line 19)
□ Initialization code present (lines 386-392)
□ 7 post-processing methods added (after line 710)
□ run_model() updated to store response (around line 716)
□ Menu option [7] added
□ Menu prompt shows [1-8]
□ Handler for choice "7" added
□ Exit moved to option [8]
□ Test run successful
□ Files appear in outputs/

═══════════════════════════════════════════════════════════════════════════════
  FILES REFERENCE
═══════════════════════════════════════════════════════════════════════════════

IMPLEMENTATION FILES:
  ✓ response_processor.py          - Core class (complete)
  ✓ outputs/                        - Storage directory (created)
  ⚠ ai-router.py                    - Needs manual edits (3 steps above)

REFERENCE FILES:
  📄 post_processing_methods.txt    - Methods to copy (Step 1)
  📄 INTEGRATION_INSTRUCTIONS.md    - Detailed integration guide
  📄 QUICK_INTEGRATION_GUIDE.txt    - This file
  📄 POST_PROCESSING_STATUS.md      - Implementation status
  📄 RESPONSE_FORMAT_EXAMPLES.md    - Usage examples

═══════════════════════════════════════════════════════════════════════════════
  COMPLETION TIME: 10-15 MINUTES
═══════════════════════════════════════════════════════════════════════════════

Step 1: 5 minutes  (copy methods)
Step 2: 3 minutes  (update run_model)
Step 3: 3 minutes  (add menu option)
Step 4: 5 minutes  (test)
Total:  ~15 minutes

═══════════════════════════════════════════════════════════════════════════════
  SUPPORT
═══════════════════════════════════════════════════════════════════════════════

For detailed explanations, see:
  - INTEGRATION_INSTRUCTIONS.md (step-by-step with context)
  - RESPONSE_PROCESSING_IMPLEMENTATION_SUMMARY.md (complete overview)
  - RESPONSE_FORMAT_EXAMPLES.md (usage examples and best practices)

═══════════════════════════════════════════════════════════════════════════════
